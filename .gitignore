# Build artifacts
target/
neuro-bridge-rs/target-repro/
__pycache__/
*.pyc

# Runtime / IPC
ipc/
neuro_outbox.jsonl
neuro_inbox.jsonl
neuro_state.json
neuro_ipc_dir.txt
game_state.json

# Logs
*.log
neuro-game/logs/

# Local config / overrides
.env
neuro.conf
neuro_tuning.env
# SMODS mod-config, written back into the tree by the game (Mods/neuro-game is a symlink here)
neuro-game/config.lua

# Test-only wrappers (not shipped)
opencode-wrapper.py
opencode-wrapper-config.json
lm-bridge.py
lm-bridge-config.json
neuro-player/
SETUP-LINUX.md
run-bridge.sh

# Offline context snapshot test harness (not shipped to the mod).
# Renders the test_deadlock scenario corpus without the game and diffs against local snapshots.
neuro-game/golden_test.lua
neuro-game/goldens/

# Local overrides
*.local.ps1
*.local.py

# Editor / OS junk
.vscode/
.idea/
.obsidian/
.claude/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Debug artifacts
*.bak*
elo.txt
action.txt
test.txt
stderr_out.txt
stdout_out.txt
opencode_stderr.txt
nul

# Local dev notes / audit reports (not shipped)
/dev/
neuro-game/dev/
# Offline rasteriser output (scripts/raster.lua): draw-op logs and PNG contact sheets.
# Names are derived from the request so re-running overwrites instead of piling up.
neuro-game/.raster/

# Local snapshot copies of the mod tree (not shipped)
neuro-game/.local-backups/
# Dev key-list scratch files (joker-key inventories, read by no code)
neuro-game/basekeys.txt
neuro-game/refkeys.txt

# luac -p byproduct
luac.out
.local-backups-archive/
